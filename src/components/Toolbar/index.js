import css from './style.module.css';

const Toolbar = () => {
  return (
    <header className={css.Toolbar}>
      <div>...</div>
      <div>Logo</div>
      <nav>Цэс</nav>
    </header>
  );
};

export default Toolbar;
